var Snake = {};

Snake.init = function() {
    this.pos = (0, 0);
    this.length = 20;
    this.direction = "up";
};

Snake.render = function() {};

Snake.update = function(delta) {};

Snake.turn = function(dir) {};
